config {
  type: "incremental",
  schema: "demo_noodles",
  uniqueKey: ["bill_id"]
}
SELECT * FROM ${ref("demo_staging_noodles","bill_facts")}
${ when(incremental(), `WHERE sales_timestamp > (SELECT IFNULL(MAX(sales_timestamp), TIMESTAMP "1970-01-01") FROM ${self()})`) }
